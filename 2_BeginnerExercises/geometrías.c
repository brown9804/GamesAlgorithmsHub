#include<stdio.h>
int main()
{
	int i;
	int j;
	int num;
	int geo;
	int creci;
	printf("Digite la cantidad de veces que quiere que se reproduzca\n");
	scanf("%d",&num);
	printf("Digite 1 si desea una fila, 2 si desea un parelogramo, 3 para una triángular y para una pirámide");
	scanf("%d",&geo);
	creci =
	if (geo==1)
	{
		for (i=num;i>0;i--)
		{
			printf("x");
		}
		printf("\n");
		return 0 ;
	}
	if (geo==2)
	{ 
		for (i=num;i>0;i--)
		{
			for(j=num;j>0;j--)
			{
				printf("x");
			}
		printf("\n");	
		}
		return 0 ;
	}
	if (geo==3)
	{
		for (i
}
